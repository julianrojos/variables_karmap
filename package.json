{
  "name": "css-variables-generator",
  "version": "1.0.0",
  "description": "Generador de custom properties CSS desde tokens JSON",
  "type": "module",
  "scripts": {
    "generate": "tsx generate-css-variables.ts",
    "build": "tsc generate-css-variables.ts --outDir dist --module esnext --target es2020 --moduleResolution node",
    "watch": "tsx watch generate-css-variables.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}

